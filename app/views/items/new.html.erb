

<%= simple_form_for @item do |f| %>
  <%= f.input :title %>
  <%= f.input :photo, as: :file %>
  <%= f.input :genre, collection: Item::GENRES %>
  <%= f.input :platform, collection: Item::PLATFORMS %>
  <%= f.submit %>
<% end %>

<%= link_to "go back", dashboard_path %>
